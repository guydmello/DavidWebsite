# The Recipe Àrchive

A responsive editorial bakery archive built with React, TypeScript, Vite, Framer Motion, and Lucide icons.

## Run locally

```bash
npm install
npm run dev
```

Use `npm run build`, `npm run lint`, `npm run typecheck`, and `npm test` for validation.
